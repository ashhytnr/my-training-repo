
const firstName :string = "myName";
const lastName :string = "myLastName";

// this is an if statement
if (firstName === "myName") {
    console.log(`First name is ${firstName}`);
}

// If statements should have an else clause
if (firstName === "myName") {
    console.log(`First name is ${firstName}`);
} else {
    console.log("Don't know your first name")
}

